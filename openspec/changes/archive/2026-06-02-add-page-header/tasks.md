## 1. Header Component

- [x] 1.1 Create `components/Header.tsx` — client component with Ant Design `Layout.Header`, title "Stock Manager" left, two icon buttons right (NotificationOutlined, SettingOutlined), dark background matching sidebar

## 2. Layout Integration

- [x] 2.1 Update `components/AppLayout.tsx` — wrap Content in nested Layout with new Header component above it

## 3. Verification

- [x] 3.1 Run `npm run build` to verify compilation
- [x] 3.2 Run `npm run lint` to check for code issues
